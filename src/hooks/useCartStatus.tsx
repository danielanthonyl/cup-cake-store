export const useCartStatus = () => {
  /**
   * here you can get the state for the cart on redux and check if its empty or not.
   * later this should be something like `cart.length < 1`
   * checking if has data on cart to either display a cart or an empty screen
   */
  const hasCartData = true;

  return hasCartData;
};
